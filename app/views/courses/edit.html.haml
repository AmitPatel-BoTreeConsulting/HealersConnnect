.page-header
  %h1
    = t('course.caption.edit_course')
    %small
      %i.icon-double-angle-right
      %b
        = @course.name
      |
      #{t('course.caption.eligibility')}:
      = render_course_eligibility(@course.eligibility)
= render :partial => 'form'