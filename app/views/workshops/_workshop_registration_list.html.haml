%tr.info
  %td.text-center<
    = workshop.start_date.strftime('%Y-%m-%d') rescue nil
  %td
    = workshop.end_date.strftime('%Y-%m-%d') rescue nil
  %td<
    = workshop.course.name
  %td<
    = workshop.instructor.name
  %td
    = workshop.center.location
  %td.text-center<
    = link_to(t('common.caption.register'), new_workshop_registration_path(workshop), class: 'green', title: t('common.caption.register'))
