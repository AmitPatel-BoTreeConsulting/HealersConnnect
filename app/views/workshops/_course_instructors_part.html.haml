.form-group
  %label.col-sm-3.control-label.no-padding-right #{t('workshop.caption.instructor')}
  .col-sm-4
    = select_tag 'workshop[instructor_id]', options_from_collection_for_select(@instructors, :id, :name),
    class: 'form-control',
    prompt: prompt_msg

.form-group
  %label.col-sm-3.control-label.no-padding-right #{t('workshop.caption.assistant_instructor')}
  .col-sm-4
    = select_tag 'workshop[assistant_instructor_id]', options_from_collection_for_select(@instructors, :id, :name),
    class: 'form-control',
    prompt: prompt_msg
