%link{href: "http://blueimp.github.io/Gallery/css/blueimp-gallery.min.css", rel: "stylesheet"}/
= stylesheet_link_tag "home/bootstrap-image-gallery.css"
#blog_post.home_course_content_margin
  .container
    .section_header
      %h3= @activity.name.capitalize
    .row
      .col-sm-8
        - if @activity.avatar.present?
          = image_tag(@activity.avatar.url(:medium), class: 'post_pic img-responsive')
        - else
          = image_tag('no_image.png')
        .post_content
          %h2= @activity.name.capitalize
          %p
            = simple_format(@activity.description)
      .col-sm-4
        .sidebar
          .box
            .sidebar_header
              %h4= t('common.caption.home_page_activity_photos')
            - if @activity.activity_photos.present?
              %ul.recent_photos
                - @activity.activity_photos.each do |activity_photo|
                  %li.activity_small_photo_height_width
                    %a{"data-gallery" => "", href: activity_photo.photo.url(:original)}
                      = image_tag(activity_photo.photo.url(:activity_small))
            - else
              %h5= t('activity_photo_gallery.message.no_photos_found')

  #blueimp-gallery.blueimp-gallery
    / The container for the modal slides
    .slides
    / Controls for the borderless lightbox
    %h3.title
    %a.prev ‹
    %a.next ›
    %a.close ×
    %a.play-pause
    %ol.indicator
    / The modal dialog, which will be used to wrap the lightbox content
    .modal.fade
      .modal-dialog
        .modal-content
          .modal-header
            %button.close{"aria-hidden" => "true", type: "button"} ×
            %h4.modal-title
          .modal-body.next
          .modal-footer
            %button.btn.btn-default.pull-left.prev{type: "button"}
              Previous
            %button.btn.btn-primary.next{type: "button"}
              Next

%script{src: "http://ajax.googleapis.com/ajax/libs/jquery/1.10.2/jquery.min.js"}
/ Bootstrap JS is not required, but included for the responsive demo navigation and button states
%script{src: "http://netdna.bootstrapcdn.com/bootstrap/3.0.2/js/bootstrap.min.js"}
%script{src: "http://blueimp.github.io/Gallery/js/jquery.blueimp-gallery.min.js"}
= javascript_include_tag "home/bootstrap-image-gallery.min.js"